class LahanParkir {
  constructor(id, nama_lahan_parkir, total_daya_tampung) {
    (this.id = id),
      (this.nama_lahan_parkir = nama_lahan_parkir),
      (this.total_daya_tampung = total_daya_tampung);
  }
}

export default LahanParkir;
