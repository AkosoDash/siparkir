class LogParkir {
  constructor(id, kd_lahan_parkir, tanggal_parkir, status) {
    (this.id = id),
      (this.kd_lahan_parkir = kd_lahan_parkir),
      (this.tanggal_parkir = tanggal_parkir),
      (this.status = status);
  }
}

export default LogParkir;
